{
  "name": "@parity/eval-harness",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Phase 0 gate — measure semantic-diff false-positive rate against a labeled ground-truth set",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run"
  },
  "dependencies": {
    "@parity/manifest": "workspace:*"
  },
  "devDependencies": {
    "@parity/fixtures": "workspace:*",
    "@parity/capture": "workspace:*",
    "@parity/escalate": "workspace:*",
    "@parity/reporter": "workspace:*"
  }
}
