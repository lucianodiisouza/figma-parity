{
  "name": "@parity/matrix",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Runtime matrix — cells as data + cartesian generator (reduced 8-cell MVP subset)",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run"
  }
}
