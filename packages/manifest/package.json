{
  "name": "@parity/manifest",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Divergence manifest (the kilobyte payload crossing MCP) + eval label schemas, zod-validated",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.24.1"
  }
}
