{
  "name": "@parity/escalate",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pass 2 — the LLM judge (intent-vs-bug) on the failing crop only. The one place a model runs",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.68.0",
    "@parity/manifest": "workspace:*",
    "@parity/diff": "workspace:*"
  }
}
