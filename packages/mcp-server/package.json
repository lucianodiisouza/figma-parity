{
  "name": "@parity/mcp-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Thin MCP control-plane server — tools over manifests only (never pixels)",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "parity-mcp": "./dist/bin.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.24.1",
    "@parity/manifest": "workspace:*",
    "@parity/eval-harness": "workspace:*"
  },
  "devDependencies": {
    "@parity/fixtures": "workspace:*"
  }
}
