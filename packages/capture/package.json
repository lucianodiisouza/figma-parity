{
  "name": "@parity/capture",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Data-plane capture contract — what the renderer produces and the diff consumes",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run"
  },
  "dependencies": {
    "@parity/ir": "workspace:*",
    "@parity/manifest": "workspace:*"
  }
}
